PORT=10000
NODE_ENV=production
# MongoDB connection string - will be set in Render dashboard
MONGODB_URI=
# JWT secret - will be set in Render dashboard
JWT_SECRET=
# Allowed origins for CORS (comma-separated URLs)
ALLOWED_ORIGINS=https://your-vercel-app.vercel.app,http://localhost:3000
